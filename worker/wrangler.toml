name = "saengginda"
main = "worker.js"
compatibility_date = "2026-08-01"

[vars]
GITHUB_REPO = "JuncholKimSo/saengginda"
GITHUB_BRANCH = "main"
ALLOWED_ORIGIN = "https://juncholkimso.github.io"

# 시크릿은 파일에 넣지 말고 아래 명령으로 등록:
#   npx wrangler secret put GITHUB_TOKEN
